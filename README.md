# DevCord
DevCord is a simple Discord API wrapper in Python. The primary goal is to make a 
wrapper with easy to parse syntax for absolute beginners of Python.
(unfortunately it convergently evolved to look exactly like discord.py -_-)