"""
Bot user initialiser

Houses the code to initialise and run a bot with the
specified token, intents, and potential prefix.
"""

class BotUser():
    """
    Initialises a bot user instance with the specified token, intents, and potential prefix.
    If no intents are specified, the standard intents are assumed.
    """
    def __init__(self, token : str, intents):
        # TO-DO: fill in default or "standard" intents later
        pass

    def slash_command(self):
        pass

    def run(self):
        """
        Runs the bot with the specified token, intents, and potential prefix.
        If no intents are specified, the standard intents are assumed.
        """
        pass